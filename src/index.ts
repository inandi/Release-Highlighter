import { ReleaseHighlighter} from './core/ReleaseHighlighter';

// Create an instance of our greeter
const foo = new ReleaseHighlighter();

// Basic hello
console.log(foo.foo());
console.log(foo.foo("TypeScript"));
